function GeneratePassword(allowLowercase, allowUppercase, allowNumbers, allowSymbols)
{
    
    return '';
}

const allowLowercase = true;
const allowUppercase = true;
const allowNumbers = true;
const allowSymbols = true;

const password = GeneratePassword(allowLowercase, allowUppercase, allowNumbers, allowSymbols);

console.log("The generated password is: " + password);